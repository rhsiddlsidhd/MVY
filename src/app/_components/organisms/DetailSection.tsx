"use client";
import React from "react";

const DetailSection = ({ data }: { data: any }) => {
  console.log(data);
  const {} = data;
  return <div>DetailSection</div>;
};

export default DetailSection;
